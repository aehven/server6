<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      html {
        /* background-color: #20243a;
        color: white; */
      }

      body {
        max-width: 40rem;
        margin-left: auto;
        margin-right: auto;
      }

      .logo {
        margin: 1rem 1rem 3rem 1rem;
      }

      .center {
        text-align: center;
        margin-left: auto;
        margin-right: auto;
      }

      .unsubscribe {
        margin-top: 3rem
      }

      a {
        color: white;
        opacity: 0.7;
      }
    </style>
  </head>

  <body>
    <div class="center logo">
      <%= image_tag(attachments["logo.png"].url) rescue nil %>
    </div>

    <%= yield %>

    <div class="unsubscribe">
      Click here to <a href="<%= ENV['DEFAULT_URL_HOST'] %>/#/unsubscribe?token=<%= @user.unsubscribe_token %>">Unsusbscribe.</a>
    </div>
  </body>
</html>
